1 p标签的多行文本要在p标签的后面添加一个. 不然文本内容会被加上<>识别成一对标签。
    p(style="color:red;").
      1e2uEDxSKVZSKhlIoQR3Ea9TdPa91xvP7Ye
      1e2uEDxSKVZSKhlIoQR3Ea9TdPa91xvP7Ye
      1e2uEDxSKVZSKhlIoQR3Ea9TdPa91xvP7Ye
2 类名和id名可以直接写在标签后面，有多个class名可以连接写多个
3 标签后面+属性,属性和属性之间使用，隔开
    p（class="p" style=""）
4 标签后面空一格 写文本内容
5 直接写一个class类名，默认的标签类型是div
6 单行的注释的话直接//，编译出来就是<!---->
  非缓冲注释//- 不会被编译出来
  块注释//-
7 有些标签的属性没有必要写=，比如input标签的checked
8 style.
  script.
9 变量声明和替换
  - var 变量=值
  #{变量名}
  变量名可以直接访问方法
  也可以从命令行传变量：--obj ‘{变量名：值}’
  文档内定义的变量名>命令行穿件来的变量名
  也可以定义一个json文件，从命令行把变量传进来，使用的命令是-o
10 转义字符和非转义字符
    #{} !{}
    =   ！=
11 流程代码
  for（var k in ...)循环之前的-不可以省略
  each value,key循环之前的-可以省略
  特别要注意首行的缩进会影响代码的执行效果
12 条件判断语句
    if
    else
    unless
13 代码重复区域块怎样复用？
    mixin
